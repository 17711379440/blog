var adminModel = function () {
/*    var initialLayuiModel = function () {
        layui.use(['layer', 'form'], function () {
            var layer = layui.layer,
                form = layui.form;
            layer.msg("加载中");
        });
    }*/

    return {
        init: function () {
            // initialLayuiModel();
        }
    }
}();
$(function () {
    adminModel.init();
});